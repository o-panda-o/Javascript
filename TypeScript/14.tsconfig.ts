/**
 * tsc --init --> It'll create tsconfig.json file(which contains all the default runtime configs)
 * tsc --> It'll run for all the .ts file in the directories with the config inside tsconfig.json
 */

class Test{
    variable1:string='variable1';
    variable2:string='variable2';
};