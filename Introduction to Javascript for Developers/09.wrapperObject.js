var greeting="Hello World";
console.log(greeting.length);
console.log(typeof greeting);
// It is a string still it works as an object
// because of wrapper object property of JS
// which is created in runtime for fraction of seconds

/**
 * Type of wrapper object:
 *  + String
 *  + Boolean
 *  + Number
 *  + Symbol
 */